# Q3 全年年末 SOC 边界：待讨论记录

**记录日期：** 2026-09-12  
**状态：** 待队长在 A/B 中确定唯一主口径；当前不授权 Cursor 启动正式全年运行或生成 `result3.xlsx`。

## 已提出的两个边界

| 方案 | 初始 SOC | 年末 SOC | 优点 | 应避免的误读 |
| --- | ---: | ---: | --- | --- |
| A：Q2 对齐 | 6000 kWh | 1200 kWh | 与 Q2 已有全年口径一致，所有 Q3 策略消耗相同的 4800 kWh 可用库存。 | 不能把成本差直接称为同一净电池库存下的纯调度收益。 |
| B：能量中性 | 6000 kWh | 6000 kWh | 年初年末库存相同，更适合比较各策略的年度运行成本。 | 不能声称题目要求年末回到初始 SOC；这是为公平比较设置的模型边界。 |

## 不可变原则

1. 两案必须在所有策略、光伏映射和结算敏感性中保持相同，不能按策略选择更有利的终端值。
2. 年末之后没有 2026 年附件数据，因此不能凭空用次日真实负荷或光伏替代终端边界。
3. 最终论文应明确主边界是建模假设，并将另一边界作为敏感性，而非题设事实。

## 建议的后续讨论问题

1. Q3 是否以与 Q2 的跨题口径可比性优先（选 A），还是以年度策略成本的能量中性优先（选 B）？
2. 若主方案确定，是否将另一案只对主策略组做敏感性，还是对全部五策略做完整验证？
