# C 题 · 小问 1 建模规格

> Codex 起草 → AI 队长确认 → Cursor 实现。确认后在文首改状态为 **已确认** 并填日期。

**状态：** 二次修订已确认（待 Cursor 实现；表 2 计量侧待最终签收）
**题目 / 小问：** C 题 Q1：给定日内电价、负荷与光伏预测的日前购电策略  
**队长确认：** 2026-09-10，90% 按公共母线侧往返效率处理并对称拆分；时间序列以附件 1 原始 144 行顺序为准，不旋转；正式结果采用 M1。

## 1. 目标

在每天 0:00 已知当天 10 分钟粒度的电价、负荷和光伏预测功率时，确定外网购电与储能充放电计划，使小区供电需求被满足、购电成本最小，且储能 0:00 与 24:00 的电量相同。

成功标准：可复现地生成题目要求的 `result1.xlsx`，所有时段能量平衡、储能边界和首末电量约束均满足。

## 2. 假设与符号

一个时段长度为 \(\Delta=1/6\) h，共 \(T=144\) 个时段。附件中的功率先乘 \(\Delta\) 转为能量。

| 符号 | 含义 | 单位 / 取值范围 |
|---|---|---|
| \(L_t\) | 小区负载能量 | kWh |
| \(P_t\) | 光伏发电可用能量 | kWh |
| \(p_t\) | 外网电价 | 元/kWh |
| \(g_t\) | 计划购电量 | kWh，\(\ge0\) |
| \(c_t,d_t\) | 接入储能的充电、向负载侧的放电量 | kWh，\(\ge0\) |
| \(E_t\) | 时段末储能电量（SOC） | kWh |
| \(s_t\) | 未被利用的光伏弃电量 | kWh，\(\ge0\) |
| \(\eta_c,\eta_d\) | 充电、放电效率 | \(\sqrt{0.9}\approx0.948683\) |
| \(\lambda_t\) | 时段 \(t\) 的 SOC 边际价值（影子电价） | 元/kWh-SOC，仅用于解释/对照 |

必要假设：

1. 题面未给出售电价格和结算规则，本文不设外网售电变量；在零光伏边际成本和无售电收益的模型下，最优解经济上等价于优先消纳光伏。未被负载或储能吸收的光伏计入 \(s_t\)，并满足 \(0\le s_t\le P_t\)。
2. 在无补贴且 \(\eta_c\eta_d<1\) 的条件下，同时充放电不会改善目标，因此不引入二元变量；实现后须检查 \(c_t d_t\) 是否近似为 0。
3. 队长确认：题中单独给出的“充放电效率 90%”按公共母线侧储能整体往返效率处理，即 \(\eta_{rt}=0.90\)。为在 SOC 递推中对称刻画单程损耗，取 \(\eta_c=\eta_d=\sqrt{0.9}\approx0.948683\)；“充、放电各 90%”（\(\eta_{rt}=0.81\)）仅作为敏感性方案，不得与主方案混用。
4. 题面未给出外网联络线/PCC 容量，主模型假设外网在给定电价下可满足购电需求，即不对 \(g_t\) 设置上界。该设定属于本文假设，不是题给事实；必须报告主方案购电峰值，并以参数化 \(G_{\max}\) 作扩展分析。
5. 队长确认：附件 1 的原始 144 行顺序是 Q1 唯一计算顺序，不按自然日时钟旋转。内部索引 \(t\) 逐行对应附件 1 第 \(t\) 行数据和官方模板第 \(t\) 个标签；六个四小时区间也按连续 24 行依次汇总。

## 3. 数据

- 来源：`C题/附件/附件1.xlsx`，工作表 `Sheet1`；结果模板为 `C题/附件/附件5/result1.xlsx`。
- 字段：时间、电价、小区负载、光伏发电预测功率。
- 预处理：严格保留附件 1 的原始 144 行顺序，不重新排序、不将末行旋转到首行；功率乘以 \(1/6\) 转换为 kWh；检查数值有限、负荷/光伏非负、价格符号、行数和首末标签。
- 文件放置：Cursor 可将原件复制到 `data/raw/c/`（不提交原始大文件），并在代码中保留来源说明。

## 4. 模型

### 4.1 变量与约束

储能最大功率为 5000 kW，因此每时段充、放电上限为

\[
0\le c_t,d_t\le 5000\Delta=833.3333.
\]

能量平衡和储能递推为

\[
g_t+P_t+d_t=L_t+c_t+s_t,
\]
\[
0\le s_t\le P_t,
\]
\[
E_t=E_{t-1}+\eta_c c_t-\frac{d_t}{\eta_d}.
\]

主方案固定 \(\eta_c=\eta_d=\sqrt{0.9}\)，故完整充放电循环满足 \(\eta_c\eta_d=0.90\)。\(c_t\) 定义为公共母线侧输入储能的电量，\(d_t\) 定义为储能向负荷侧输出的电量。

并满足

\[
1200\le E_t\le10800,\qquad E_0=6000,\qquad E_{144}=E_0.
\]

### 4.2 目标

\[
\min\ \sum_{t=1}^{144}p_tg_t.
\]

这是线性规划（LP），用 HiGHS 求解。队长确认正式结果采用 **M1**：只最小化购电费，不增加第二阶段吞吐量目标。M2 仅保留为数值形式对照，不生成正式 `result1.xlsx`。

## 5. 算法

### 5.1 主算法：线性规划

- 正式提交结果以第 4 节 LP 为准。该模型的目标、能量平衡、SOC 递推及所有边界均为线性约束，能够在 144 个时段上取得全局最小购电成本。
- 推荐：`cvxpy` 建 LP，优先 `HIGHS`；求解失败时再尝试 `CLARABEL`。备选为 `scipy.optimize.linprog(method="highs")`。
- 实现顺序：数据读取 → 单位转换 → 约束建立 → 求解 → 残差检查 → 导出。
- 不以固定“低价/高价时段比例”决定主模型充放电顺序；该类分位数规则具有主观性，只能作为经验基线。

### 5.2 最优调度的规则解释：影子电价阈值

为避免 LP 成为难以解释的黑箱，用 SOC 的边际价值 \(\lambda_t\) 解释最优结果。对电网侧 1 kWh 充电能量，获得 1 kWh SOC 的成本为 \(p_t/\eta_c\)；对 1 kWh SOC 放电，其可避免的购电成本为 \(p_t\eta_d\)。在由外网边际供能且相关功率、SOC 和弃光边界未激活的内点条件下，最优性条件对应如下经济规则：

\[
\begin{cases}
\text{倾向充电}, & \frac{p_t}{\eta_c}<\lambda_t,\\
\text{倾向放电}, & p_t\eta_d>\lambda_t,\\
\text{保持}, & p_t\eta_d\le\lambda_t\le\frac{p_t}{\eta_c}.
\end{cases}
\]

中间的保持区间是充放电损耗造成的无操作区，并非人为设定；边界激活时须结合 KKT 互补条件解释。将外网电量从充电时段 \(i\) 搬运至放电时段 \(j\) 的必要套利条件为：

\[
p_j\eta_c\eta_d>p_i.
\]

\(\lambda_t\) 由未来价格、净负荷、SOC 边界和日末 \(E_{144}=6000\) 共同决定；在 LP 中由其对偶变量/KKT 条件体现。论文中以该规则解释结果中的低价充电、高价放电和保持时段，不将其另作主观阈值算法。

光伏盈余的即时机会成本为零：在不挤占后续更高价值储能机会且电池有可用容量时，优先用于充电；否则弃光。是否保留容量给未来光伏或套利机会，同样由 \(\lambda_t\) 和 SOC 边界决定，不能简化为“光伏盈余必然充满”。

### 5.3 独立对照：动态规划

若需要与 LP 进行独立算法交叉验证，采用 SOC 离散化的动态规划（DP），而非固定分位数规则。令 \(V_t(E)\) 为从时段 \(t\) 开始、SOC 为 \(E\) 时的最小未来购电成本：

\[
V_t(E)=\min_{c_t,d_t,g_t,s_t}\left\{p_tg_t+V_{t+1}\left(E+\eta_c c_t-\frac{d_t}{\eta_d}\right)\right\},
\]

并施加第 4 节约束及终端条件 \(V_{145}(6000)=0\)。DP 的 SOC 网格精度须记录；它在网格内近似全局最优，可用于核对 LP 成本、SOC 轨迹和边界约束。连续 LP 仍是正式输出的权威来源。

### 5.4 经验基线（仅在需要比较时使用）

若论文需要展示优化收益，可另设固定低/高电价分位数的顺序规则：光伏优先、低价购电充电、高价净负荷放电、最后回调 \(E_{144}=6000\)。必须明确标注为“经验基线策略”，报告其分位数参数和日末 SOC 回调过程；不得称为最优规则或用于生成正式 `result1.xlsx`。

### 5.5 模型形式与词典序目标的内部对照

在正式导出前运行以下四种模型，作为实现验收而非默认论文实验：

| 编号 | 充放电互斥 | 第二阶段目标 | 用途 |
|---|---|---|---|
| M1 | 不加二元变量（LP） | 无 | **正式主方案** |
| M2 | 不加二元变量（LP） | 在成本最优下最小化 \(Q=\sum_t(c_t+d_t)\) | 数值择优对照，不作寿命结论 |
| M3 | 加二元变量（MILP） | 无 | 检验显式互斥的必要性 |
| M4 | 加二元变量（MILP） | 在成本最优下最小化 \(Q\) | 检验 M2 是否已足够 |

M3、M4 使用 \(z_t\in\{0,1\}\) 约束：

\[
c_t\le C_{\max}z_t,\qquad d_t\le D_{\max}(1-z_t).
\]

M2、M4 必须采用词典序求解，而非任意小权重加权：先求购电成本 \(C^*\)，再加入 \(\sum_tp_tg_t\le C^*+\varepsilon_C\)，最小化 \(Q\)。\(\varepsilon_C\) 仅取求解器数值容差量级并在校验文件中记录。

四种模型统一比较 \(C=\sum_tp_tg_t\)、总购电量 \(G\)、弃光量 \(S\)、吞吐量 \(Q\)、最大 \(c_t d_t\)、SOC 极值、能量平衡残差和求解时间。队长已选 M1 生成正式结果；M2–M4 不得覆盖正式输出。若 M1 与 M3 成本相同且 M1 的同时充放电仅为数值残差，则不保留二元变量。论文只将 M1–M4 称为同一数据和求解器下的模型形式对照，不称为独立算法交叉验证。

- 输出时间：`result1.xlsx` 的“计划购电量”恰有 144 行，标签从 `0:10-0:20` 至 `0:00+1-0:10+1`。队长确认以附件 1 原始行序为准：内部第 \(t\) 个结果逐行写入模板第 \(t\) 行，不旋转、不新增 `0:00-0:10`、不删行或修改官方标签。
- 四小时汇总：按附件原始行序每连续 24 行分组，即 \(t=1{:}24,25{:}48,\ldots,121{:}144\)，分别写入模板六个既有区间。论文须说明这是官方附件行序口径，不再尝试把数据旋转到自然日时钟。

## 6. 输出（给 Cursor）

| 文件 | 内容 | 列名 / 格式 |
|---|---|---|
| `output/q1_plan.csv` | 完整 144 时段的可对账结果 | `time, price, load_kwh, pv_kwh, grid_purchase_kwh, charge_kwh, discharge_kwh, soc_kwh, curtailment_kwh, balance_residual_kwh` |
| `output/q1_summary.csv` | 全天总购电量、总费用、首末 SOC、弃电量 | `metric, value, unit, meaning` |
| `output/q1_baseline.csv` | 无储能基准与 M1 的购电量、费用、弃光和节省比例 | 统一单位与来源 |
| `output/q1_sensitivity.csv` | 效率 A/B、容量 ±10%、功率 ±10% 与 PCC 参数分析 | 每个情形独立一行，不覆盖主方案 |
| `fig/fig1_dispatch.png` | 负荷、光伏、购电与充放电日内曲线 | 标注单位和图例 |
| `fig/fig1_soc_price.png` | SOC 与电价关系图 | 双轴或上下子图 |
| `result1.xlsx` | 按附件 5 官方模板填写 | 保留“计划购电量”“充放电量”工作表、144 行时间标签和原表头 |

## 7. 验证

- 逐时段检查能量平衡残差绝对值 \(<10^{-6}\) kWh。
- 检查所有 SOC、充放电量均在边界内，且 \(|E_{144}-E_0|<10^{-6}\) kWh。
- 逻辑检查：低价/光伏富余期倾向充电，高价期倾向放电；异常情况必须解释。
- 交叉验证（若启用 DP）：记录 SOC 网格精度，比较 DP 与 LP 的目标值、首末 SOC 和约束残差；差异须处于预设网格误差范围内。
- 模型形式对照：按第 5.5 节输出 M1–M4 的统一比较表；第二阶段目标不得使购电成本超过第一阶段最优值加记录的数值容差。
- 灵敏度：必须比较主方案 A（\(\eta_{rt}=0.90\)，\(\eta_c=\eta_d=\sqrt{0.9}\)）与方案 B（\(\eta_c=\eta_d=0.90\)，\(\eta_{rt}=0.81\)）；扩展分析可再对往返效率 \(\eta_{rt}\in\{0.85,0.90,0.95\}\) 采用对称拆分。容量与最大功率各上下浮动 10%。
- 报告 M1 的最大购电功率；PCC 扩展只做参数分析，不将任意外网容量写成题给参数。
- `result1.xlsx` 的表 2 暂继续汇总公共母线侧 \(c_t,d_t\)，并在验证文件记录计量侧；队长最终签收前不得在论文中省略该定义。

## 8. 论文要点

- 先解释 10 分钟离散化与“功率转电量”，再给出能量守恒与 SOC 递推。
- 强调首末 SOC 相等避免通过透支储能虚假降低当天成本。
- 报告全天购电量、购电费、削峰充放电时段和弃光情况。

## 9. 待队长确认项

- [x] 效率口径：90% 为公共母线侧往返效率，主方案 \(\eta_c=\eta_d=\sqrt{0.9}\)；充、放电各 90% 仅作敏感性方案（2026-09-10）。
- [x] 时间口径：以附件 1 原始 144 行顺序计算和汇总，不旋转；结果按官方模板行序逐行填写（2026-09-10）。
- [x] 正式模型：采用 M1，不以 M2 生成正式结果（2026-09-10）。
- [ ] 表 2 计量侧最终签收：暂按公共母线侧充电输入 \(c_t\) 和放电输出 \(d_t\) 汇总。
