# C 题 Q3 因果负荷修订复核

**复核对象：** `cursor/c-q3-pilot-implementation-730d`，提交 `fd78c52`、`7d11a5f`。  
**复核结论：** RQ3-1（同日未来负荷信息泄漏）已关闭。`causal_load_main` 可作为 Q3 现实信息结构下的**两日试算主口径**；`actual_load_proxy` 已被正确降格为完美负荷信息对照。尚不可生成全年 `result3.xlsx`，须先完成下列年度化与敏感性事项。

## 1. 本次通过项

| 项目 | 复核证据 | 结论 |
| --- | --- | --- |
| 负荷信息边界 | 主口径在 0:00 取严格早于目标日的最近至多 4 个同星期日负荷均值；6/12/18 的未来负荷保持该日 0:00 预测；每个 10 分钟执行步仅将当前真实负荷代入。 | 通过 |
| 因果扰动 | `q3_causality_audit.json` 将 2025-02-01 06:00 后尚未发生的负荷统一扰动 500 kWh。主口径日前预测、更新预测、次日预测与 (g^0) 均保持不变；代理情景按预期变化；`all_pass=true`。 | 通过 |
| 单元测试与环境 | 在项目 `.venv`（Python 3.13、cvxpy 1.9.2、HiGHS）执行 `python -m unittest q3.test_q3 -v`，12/12 通过；其中含同日未来负荷不影响主预测和 (g^0) 的测试。 | 通过 |
| 结算复算 | 按 φ\(_t=p_tg_t^F+0.5p_t\lvert g_t^F-g_t^0\rvert\) 从 12 个 `causal_load_main` 调度文件独立复算。结算、紧急购电与汇总总成本最大差异均不超过 \(3.64\times10^{-12}\) 元。 | 通过 |
| 物理与锁定 | 独立复算的最大能量平衡残差为 \(3.41\times10^{-13}\) kWh，SOC 递推残差为 \(4.83\times10^{-8}\) kWh；所有文件 SOC 均在 [1200, 10800] kWh 内，0:00 锁定段改写数为 0。 | 通过 |
| 输出与图 | `causal_load_main`/`actual_load_proxy` 在 CSV、汇总和图例中明确区分；主图将实际负荷与因果负荷 hat 并列展示。 | 通过 |

两日主口径中，M1/M6 相对 M0 的实现成本差为：2025-02-01 的 \(-889.46\) 元、2025-06-21 的 \(-2774.71\) 元。它们只用于验证链路和说明日别差异，**不可**外推为全年策略排序。

## 2. 进入全年计算前必须关闭的事项

| 编号 | 事项 | 必要动作 |
| --- | --- | --- |
| RQ3-2 | 小时预测映射到 10 分钟的线性插值仍是模型选择，不是附件唯一指定。 | 增加分段常数映射的全年敏感性，报告与线性映射在总成本、紧急购电、弃光和策略排序上的差异。 |
| RQ3-3 | 两日试算中 2 月 1 日采用 M0 预热、6 月 21 日以 6000 kWh 孤立启动；这不能替代策略各自的全年状态路径。 | 每个策略从 1 月 1 日 6000 kWh 起独立滚动全年并连续传递 SOC；比较时不得共享另一策略的 SOC 路径。 |
| RQ3-4 | 以 0:00 原计划为唯一结算锚点且不累加中间版本，是可审计但非题面唯一的结算解释。 | 以“相邻版本累积调整/字面重复计费”作为替代结算解释完成全年敏感性，并在论文中明确主口径是解释选择。 |
| RQ3-5 | `q3_cost_audit.csv` 缺少 `with_terminal_value` 字段，故同一日期、策略、信息口径下的“有/无 48h”记录不能只凭行字段唯一关联汇总表。 | 在全年运行前将该字段写入成本审计，并让验证器逐文件/逐行核对汇总成本。这是可追溯性修正，不影响本次已复算通过的数值。 |

## 3. 使用边界

1. 全年正式主结果只可使用 `causal_load_main`；`actual_load_proxy` 仅用于量化完美负荷信息的理想化对照，不能混入主结果均值、图表或结论。
2. “更新更优”“M1/M6 最优”等论述必须以全年、相同初始条件、同一结算口径的统计结果为准；两日结果不得用于定策略。
3. 本记录只关闭 RQ3-1，不替代跨题证据链清单中关于结算解释、插值和论文措辞的最终整理。
