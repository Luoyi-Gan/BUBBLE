# ACCT 任务单：C 题 Q1 论文骨架与对账

**阶段目标：** 按签收单建立四小问论文骨架，并优先完成 C 题 Q1 的可审计写作准备。  
**负责人：** ACCT  
**协作对象：** AI 队长（唯一数字/口径确认人）、FIN（证据与结果解读）、Codex（规格与逻辑审阅）、Cursor（代码和 `output/` 产出）。  
**范围：** 论文、对账表、图表占位和格式；不改模型、代码、`output/` 数字或 `docs/specs/`。

## 一、唯一口径与禁止事项

1. 模型文字以 `docs/specs/q1.md` 为准；草稿内容必须标注“待队长确认”直到规格状态变为已确认。
2. 论文中的任一数值必须同时能定位到队长已确认的 `results_summary.md` 和 `output/` 文件；Cursor 刚生成但未签收的 CSV 一律标“未核”，不得写进正文、摘要或结论。
3. 不修改 SOC、效率、能量单位、价格或时间区间的定义。发现冲突时，在对账表标红并交队长，不自行折中。
4. Q1 没有结果前可以写结构、符号、公式占位与图注草稿；不得杜撰趋势、费用、购电量或“明显改善”等结论。

## 二、当前优先任务：9.10 晚（18:00–22:00）

### A1 建立四小问论文骨架

在 Overleaf 建立以下顺序的章节，并在本仓 `paper/` 同步大纲或备份：

1. 摘要（仅占位）
2. 问题重述
3. 问题分析
4. 模型假设
5. 符号说明
6. 模型建立与求解（Q1、Q2、Q3、Q4）
7. 结果分析与模型检验
8. 模型评价与推广
9. 参考文献
10. 附录与支撑材料说明

**签收标准：** 章节均可编辑，Q1–Q4 均有独立小节；任何数字尚未填入。

### A2 建立 C 题 Q1 表图清单

按题面创建下表，标明“数据来源”和“当前状态”。

| 编号 | 论文内容 | 题面要求/来源 | 当前可写内容 |
|---|---|---|---|
| Q1-表1 | 指定 10 分钟购电量、全天购电量与购电费 | `result1.xlsx` 的计划购电量；队长确认的 `output/q1_*` | 表头与单位；数值留空 |
| Q1-表2 | 6 个四小时区间的充/放电量、0:00 与 24:00 SOC | `result1.xlsx` 的充放电量；队长确认输出 | 表头与单位；数值留空 |
| Q1-图1 | 负荷、光伏、购电和充放电的日内调度 | `fig/fig1_dispatch.png` | 图位和图注草稿 |
| Q1-图2 | SOC 与电价的时序关系 | `fig/fig1_soc_price.png` | 图位和图注草稿 |
| Q1-检验表 | 能量平衡、SOC 边界、首末 SOC 一致性 | `output/q1_summary.csv` / 验证字段 | 表头；数值待核 |

**签收标准：** 每张表/图有编号、单位、数据来源、正文引用位置；未核数据不进正文。

表 1 的固定时段为：10:00–10:10、12:00–12:10、14:00–14:10、16:00–16:10、18:00–18:10、20:00–20:10。
表 2 的固定汇总区间为：0:00–4:00、4:00–8:00、8:00–12:00、12:00–16:00、16:00–20:00、20:00–24:00。

### A3 建立对账表 v0

新建 `paper/q1_reconciliation.md`，每一行对应论文中的一个数值或结论，字段固定为：

| 论文位置 | 指标/文字 | 单位 | 展示精度 | `output/` 文件与列 | `results_summary.md` 条目 | 模板状态 | 售电/弃光口径 | 状态 | 发现人 | 队长签收时间 |
|---|---|---|---|---|---|---|---|---|---|---|

状态只能填：`空缺`、`未核`、`已核`、`不一致`。当 Cursor 提供输出后，先填文件与列名；只有队长确认后才能改为 `已核`。

- 展示精度：内部计算保留全精度；只有题面或附件模板明确时才按指定规则展示/四舍五入，不能自行截断。
- 模板状态：填写 `未取得`、`待核对`、`已核对`；题包当前未发现附件 5 的 `result1.xlsx` 模板，取得前不得猜测工作表名或列位置。
- 售电/弃光口径：暂填“无售电价格，富余光伏可弃用（待队长确认）”；FIN F5 签收后再改为已核口径。

**签收标准：** Q1-表1、Q1-表2、全天费用、首末 SOC、两张图、三项检验均已有一行对账记录。

## 三、深夜写作任务：9.10 22:00–02:00

### A4 问题重述 v0

写 250–350 字：说明微网由负荷、光伏、储能和外网组成；目标是日前制定购电及充放电策略，在满足负荷和储能边界下最小化购电费。不得自行补充住户数量、售电机制、设备类型或未来数据。

### A5 符号与公式核对框架

依 `docs/specs/q1.md` 建符号表，至少包含 \(\Delta,L_t,P_t,p_t,g_t,c_t,d_t,E_t,s_t,\eta_c,\eta_d\)。

- 功率单位为 kW，进入优化前乘 \(\Delta=1/6\) h 转为 kWh；表内不得混写。
- `g_t`、`c_t`、`d_t` 均为一个 10 分钟时段的能量，不是功率。
- 所有公式先标“待队长确认效率口径”；FIN 的 F2 证据包签收前，不把 90% 的解释写成定论。

**签收标准：** 问题重述和符号表完成；无未经签收数值；可由 Codex 逐项对照规格。

## 四、后续签收节奏

| 时间 / 阶段 | ACCT 必做事项 | 交付与验收 |
|---|---|---|
| 9.11 早 | 导出 Overleaf PDF 备份；Q1 输出存在时逐项登记为“未核” | PDF 备份 + 对账表无遗漏 |
| 9.11 13:30–16:00 | 参加演示会；当场检查 Cursor 的 CSV 列名、单位和合计；队长签收后写 Q1 正文 v1 | 表图数字与 `output/` 一致；不一致项标红 |
| 9.11 16:00–24:00（队长离场） | 主持对账；维护未核标记；写问题重述、符号说明、图注和排版 | 新 CSV 均为“待 24:00 签收”，不写数字结论 |
| 9.11 24:00–02:00 | 依据队长夜间签收修正正文和对账表 | 未核数字删除或转已核 |
| 9.12 | 逐问接收已核结果；完成图表编号、交叉引用、摘要草稿和第二轮全篇对账 | 论文、`output/`、`results_summary.md` 无红项，或有明确清单 |
| 9.13 08:00–13:00 | 检查 PDF、页数、参考文献、支撑材料；与队长完成最终数字核对并上传 | 提交回执、PDF、代码包三份备份 |

## 五、Q1 正文写作顺序

1. 问题重述与系统边界。
2. 假设：仅引用 FIN 已签收的 F1–F5 证据包；题给约束、本文假设与未考虑因素分开写。
3. 符号说明与 10 分钟离散化。
4. 能量平衡、SOC 递推、功率/容量边界、首末 SOC 约束、成本目标。
5. 求解方法：线性规划与求解器，不夸大算法复杂度。
6. 表 1、表 2 与图 1、图 2 的结果描述，仅填已核数字。
7. 检验：能量平衡残差、SOC 边界、首末 SOC 一致性；这些不是宣传性结果，而是模型有效性检查。

## 六、向队长汇报的固定格式

> **阶段：** A1 / A2 / A3 / A4 / A5 或日期时段  
> **已完成：** 文件路径、表图编号或段落名称。  
> **未核数字：** 指标 + `output/` 文件/列。  
> **不一致：** 论文位置 vs 输出位置，具体数值/单位。  
> **需要确认：** 规格、文献、数字或格式问题。  
> **签收：** 完成时间 / 验收人。

## 七、ACCT 签收清单

- [ ] Overleaf 四问骨架和本仓备份已建立。
- [ ] Q1 表图清单、图位和图注草稿完整。
- [ ] `paper/q1_reconciliation.md` 已建立并含全部 Q1 数值槽位。
- [ ] 表 1、表 2 的固定时段和汇总规则已核对；展示精度未被擅自改写。
- [ ] `result1.xlsx` 模板状态已登记；未取得模板时没有臆造工作表或列位置。
- [ ] FIN F5 的售电/弃光及外网购电边界已写入“模型假设/局限性”对应位置。
- [ ] 问题重述 v0、符号表 v0 完成且无未核数字。
- [ ] 所有结果都能追溯到队长确认的 `output/` 与 `results_summary.md`。
- [ ] 队长离场窗口内没有把新 CSV 写入正文。
