# 2026-09-12 模型收口任务提取

**状态：** 进入模型收口。本文件从最新未合并分支抽出待办，供队长拍板后执行。  
**提取时刻：** 2026-09-12（`main` = `34981b3`；最新 Codex = `048d5c2`；最新 Cursor 计算 = `c7f886e`）。  
**硬约束：** 不再开新核心模型（M5、CVaR 进目标、GARCH/深度学习、完整多阶段情景树均不启动）。16:00 后只修 bug、导出、审计、论文口径。

## 0. 一句话结论

四问的**主模型已经齐**，但 `main` 仍停留在 Q1 签收 + 旧 Q2 + Q2 政策一致 C2-R2 + Q4 讨论规格。真正可写论文的 Q3/Q4 全年结果在未合并 PR 上，且 **Q4-2 还不能当最终结论**，因为政策一致 Q2（C2-R3）尚未全年放行。

| 小问 | 模型是否冻结 | 正式文件是否在 `main` | 论文能否直接写数字 |
| --- | --- | --- | --- |
| Q1 | 是（M1，已签收） | 是 `result1.xlsx` | 是 |
| Q2 旧主方案 | 是（已签收 `result2.xlsx`） | 是 | 是（V1） |
| Q2 政策一致 V2 | 规格已定；C2-R3 未跑完 | 否 | 否；不得替换 `result2.xlsx` |
| Q3 | 主口径已验收（A + `M1_M6`） | 否，在 PR #13/#14 | 待合并 + 队长签收 |
| Q4-3 | 规格已冻；全年已跑 | 否，在 PR #17 | 待导出清洁 + 队长签收 |
| Q4-2 | 规格已冻；全年已跑 | 否，在 PR #17 | **暂不可作终稿**；须等 Q2 C2-R3 桥接 |

## 1. 最新分支与任务来源

按提交时间，收口期只认下面六条来源；更早的试点 PR 只作历史。

| 来源 | 提交 / 分支 | 抽出的任务性质 |
| --- | --- | --- |
| Codex 最新 | `048d5c2` `codex/c-q4-quant-finance-discussion` | FIN 收口计算单：立即复现 Q4 两日；Q4-2 全年重跑有前置条件 |
| FIN 证据 | `a5f7079` `codex/fin-evidence-chain-audit` | Q1–Q4 主张分类与禁用句；**部分条目已被后续签收覆盖，见 §5** |
| Cursor Q4 P2 | `c7f886e` PR [#17](https://github.com/Luoyi-Gan/BUBBLE/pull/17) | Q4-2/Q4-3 两日 + 365 日 + `result4-*.xlsx` |
| Cursor Q3 导出 | `08b0c5d` PR [#14](https://github.com/Luoyi-Gan/BUBBLE/pull/14) | `result3.xlsx` + 因果价格审计（P0/P1 已声明完成） |
| Cursor Q3 全年 | `6e16b3a` PR [#13](https://github.com/Luoyi-Gan/BUBBLE/pull/13) | A/B 年末 SOC 3650 日；主策略排序稳定 |
| `main` Q2 V2 | `53d8761` 已合并 | C2-R1/R2 完成；**C2-R3/R4 仍是收口期最大计算缺口** |

未并入 `main`、但已有产物、收口时不要重做的计算链：

```text
PR #13 全年 A/B
  → PR #14 result3.xlsx + 价格模块
    → PR #17 Q4-2 / Q4-3 全年
```

论文侧仍开着 PR [#9](https://github.com/Luoyi-Gan/BUBBLE/pull/9)、[#10](https://github.com/Luoyi-Gan/BUBBLE/pull/10)（Q1–Q2 问题重述 / 问题分析）。试点 PR #6/#11/#12 不要再当当前任务。

## 2. 已冻结、不得再改的口径

### Q1（队长已签收）

- 附件 1 原始 144 行顺序；效率口径 A：\(\eta_c=\eta_d=\sqrt{0.9}\)；正式模型 M1。
- \(E_0=E_{144}=6000\) kWh，SOC 1200–10800 kWh；不售电；主模型无 PCC 上限。
- 表 2 公共母线侧 \(c_t/d_t\)。数字：购电费 **33801.495542** 元。
- 论文只称“一个最优购电计划”，不称逐时轨迹唯一。

### Q2 V1（队长已签收，可写论文）

- 日前锁 \(q\)、\(x\le q\)、按 \(q\) 计费、紧急电 \(5p e\)、跨日 SOC、K=8 风险储备。
- 正式文件仍是现有 `output/result2.xlsx`。政策一致重设计**不得覆盖**该文件，除非 C2-R3 通过且队长明确替换。

### Q3（Codex 年度复核已放行主策略；`result3.xlsx` 在 PR #14）

| 项目 | 冻结值 |
| --- | --- |
| 年末 SOC | A / 1200 kWh；B=6000 只作已完成敏感性 |
| 主策略 | `M1_M6`（与 M1 不是两个独立创新） |
| 负荷 | `causal_load_main` |
| 光伏映射 | `linear_anchor_main` |
| 结算 | `anchor_final_main`（相对 0:00 的 \(g^0\)） |
| 1–12 月主成本 | 16,373,508.75 元（相对 M0 −4.30%） |

禁止：重跑 10 条年度路径；重选策略或年末 SOC；把两日映射/结算敏感性写成“全年稳健”。

### Q4 规格（`docs/specs/q4.md` 第 10 节已全部勾选）

规格已冻结，讨论稿 §8 里未勾的 D3/D4 **以规格为准，不再讨论**：

- D1：因果价格，当天未来附件 4 不得泄漏。
- D2：调整费按交付时段 \(p_t\)；\(p_\tau\) 只做敏感性。
- D3：主目标仍是实际购电成本；CVaR 只报告，不进目标。
- D4：主模型用价格—负荷—光伏三元残差路径，不先做价格独立简化版。

禁止：另写全知价格；用 Q4 成本重选 Q3 策略；把 2–12 月工作簿合计写成年度主结论；把 Q4 成本与 Q3 的 16,373,508.75 元直接比较（电价附件不同）。

## 3. 未签收的候选数字（ACCT / FIN 不得当终稿）

来自 PR #17 `c7f886e`，物理/账本/信息集两日审计声明通过。FIN 收口单明确：**不得把 17,680,437.93 元写成最终 Q4-2 结论**。

| 系统 | 1–12 月实际成本 | 2–12 月工作簿 | 正式文件（均在 PR 分支） |
| --- | ---: | ---: | --- |
| Q4-2 `p q + 5 p e` | 17,680,437.93 | 15,257,282.59 | `output/result4-2.xlsx` |
| Q4-3 \(\phi+5pe\)，`M1_M6` | 17,489,808.24 | 15,346,418.14 | `output/result4-3.xlsx` |
| Q3 主路径（对照） | 16,373,508.75 | 14,512,748.53 | `output/result3.xlsx` |

两日试点（与全年流一致）：2025-02-01 Q4-2 / Q4-3 = 19,757.46 / 18,917.03；2025-06-21 = 22,322.36 / 21,776.98。滚动 \(\alpha\) 全年停在 0.60。年末 SOC 两边都打到 1200 kWh。

## 4. 收口任务总表（按优先级）

### P0：今天必须由队长拍板

| ID | 决策 | 推荐（已有证据） | 不拍板的后果 |
| --- | --- | --- | --- |
| C-1 | Q3 主数字与 `result3.xlsx` 是否签收 | 签收 PR #13+#14 口径；1–12 月用 16,373,508.75 | ACCT 不能写 Q3 |
| C-2 | Q2 论文用 V1 还是等 V2 | **主文继续 V1**；V2 作“政策一致对照”，C2-R3 来不及或未过就不替换 `result2.xlsx` | Q4-2 桥接无限期；论文无 Q2 终稿 |
| C-3 | Q4-3 是否可在导出清洁后签收 | 可以：不依赖 Q2 V2；固定 `M1_M6` | Q4 只剩半问 |
| C-4 | PR 合并顺序 | `#13 → #14 → #17`，再并本收口文档与 FIN 证据 | `main` 继续缺 Q3/Q4 代码 |
| C-5 | 讨论稿 D3/D4 勾选 | 与规格第 10 节对齐，勾上即可 | FIN 证据链仍把 D3/D4 写成待决 |

### P0：Cursor 立即做（不改模型）

| ID | 任务 | 完成标准 | 禁止 |
| --- | --- | --- | --- |
| K-1 | 把 PR #13/#14/#17 按依赖链合并进 `main`（或由队长点合并） | `q3/`、`q4/`、`result3.xlsx`、`result4-*.xlsx` 在默认分支可检出 | 不重跑全年 |
| K-2 | Q4-3 导出清洁：列宽、2–3 位小数、紧急购电只列大于数值容差的正值 | `result4-3.xlsx` 仍 334×144；展示过滤不改账本/年成本 | 不重跑 365 日 |
| K-3 | Q4 正式图（handoff：全年通过后才画） | 成本分解、紧急购电、SOC、价格预测误差、尾部日成本；2–12 与 1–12 分栏 | 不把工作簿合计当年度主结论 |
| K-4 | 若 C-2 选择“主文 V1”：给 Q4-2 加醒目标注 | 元数据写明：当前 Q4-2 继承的是已签收旧 Q2 政策，不是 C2-R3 | 不把 PR #17 数字改称 V2 |

### P0：FIN 立即做

| ID | 任务 | 来源 | 完成标准 |
| --- | --- | --- | --- |
| F-1 | 独立 worktree 复现 `c7f886e` 两日回归 | `docs/handoff/fin-closeout-compute.md` 任务 A | 测试与 `run_q4_twoday.py` 退出码 0；三份 `all_pass=true`；不改 Excel |
| F-2 | 用已签收口径修订证据链过时条目 | `refs/c_q1_q4_evidence_chain_final_audit.md` | D1/D2 改为已确认；D3/D4 改为规格已冻；Q4 数字仍标“候选待签收” |

### P1：仅当队长选择替换 Q2 主口径时才启动

缺一不可，见 FIN 收口单任务 B：

1. Cursor 提交 Q2 C2-R3：365 日主路径、K=4/8/12 闭环敏感性、2 月初 SOC 敏感性；`validate_q2_policy_consistent.py` 零错误；**仍不**自动导出新 `result2.xlsx`。
2. Codex 年度复核 + 队长确认是否替换正式 Q2。
3. Cursor 提供 **Q4-2 桥接 commit**（最终 Q2 的因果预测、日前承诺、风险储备、日内执行），先过 Q4-2 两日。
4. FIN 在桥接 commit 上只跑 `q4_2` 全年并导出 `result4-2.xlsx`。
5. C2-R4 才允许生成候选新 `result2.xlsx`，旧文件留可回退副本。

若 C-2 选择主文保留 V1：**整段 P1 降为附录/对照，不阻塞初稿。**

### P1：ACCT（只用已签收数字）

| ID | 任务 |
| --- | --- |
| A-1 | 合并论文 PR #9/#10 的 Q1–Q2 重述与分析 |
| A-2 | Q1 按 `results_summary.md` 对账；Q2 按现有 `result2.xlsx`（V1） |
| A-3 | Q3 只在 C-1 签收后写入；区分 1–12 年成本与 2–12 工作簿 |
| A-4 | 每项假设标 F/M/A/E/L；套用 FIN 禁用句（§5） |
| A-5 | Q2 正文定位为“共享日前额度的情景规划 + 因果滚动执行”，不称严格多阶段随机最优 |

### P2：有时间才做，写不进主结论

| ID | 任务 | 正确表述 |
| --- | --- | --- |
| X-1 | Q3 映射×结算全年四组合（五策略，A/1200） | 未跑完前禁止写“全年排序稳健” |
| X-2 | Q4 价格 oracle、\(p_\tau\) 结算敏感性 | 主模型物理签收之后；oracle 不称下界 |
| X-3 | Q4 日成本 P90/P95、CVaR\(_{0.90}\)、最大日成本、紧急次数 | 只报告，不改调度 |
| X-4 | Q3 M5 条件残差情景树 | **本赛程不做** |

## 5. FIN 证据链需要立刻改写的过时判断

`refs/c_q1_q4_evidence_chain_final_audit.md` 完成于 Q4 P2 归档之前，与最新规格/讨论冲突。收口时以左边为准：

| 证据链原文 | 2026-09-12 事实 |
| --- | --- |
| D1/D2 待队长决策 | 讨论稿已于 2026-09-12 勾选；规格第 10 节已冻 |
| D3/D4 待决 | 规格已勾选：CVaR 不进目标；三元残差为主模型 |
| “Q4 尚未授权实现 / 无正式调度” | PR #17 已有全年账本与 `result4-*.xlsx`，但仍是未签收候选 |
| “当前分支无 result3.xlsx” | PR #14 已有；`main` 仍无 |
| V2 `deployed_days=365` 不是全年正式运行 | 仍正确：那是 C2-R2 校准审计，C2-R3 未完成 |

继续有效的禁用句（必须进论文）：不写题目禁止售电、PCC 无限、K=8 最优、M1 与 M6 独立创新、oracle 严格下界、运行成本差=投资收益、Q4 与 Q3 年成本直接对比。

## 6. 建议执行顺序（收口日）

1. 队长签 C-1–C-5（五分钟决策表）。
2. 合并计算链 PR #13 → #14 → #17，再合并本收口文档。
3. FIN 做 F-1 两日独立复现；Cursor 做 K-2/K-3（Q4-3 导出 + 图）。
4. ACCT 用 Q1 + Q2 V1 推进正文；Q3 在 C-1 后写入。
5. 仅当 C-2 选择替换 V2 时才启动 C2-R3；否则 Q4-2 按 K-4 标注为“继承已签收 Q2 政策的候选”。
6. 提交前闸门：`docs/reviews/physical-meaning-final-audit.md` 全题签收表。当前 Q1/Q3/Q4 在该表上仍是“待最终审计”。

## 7. 给下一棒的最短指令

**Cursor：** 不改 `docs/specs/`，不重跑 Q3 十路，不重跑 Q4 价格模块。先合并/对齐 PR 链，再修 `result4-3.xlsx` 展示层并补 Q4 图。只有队长书面要求替换 Q2 时才跑 C2-R3。

**FIN：** 先按 `docs/handoff/fin-closeout-compute.md` 任务 A 复现两日；任务 B 在桥接 commit 出现前不要动。同步改证据链里过时的 D1–D4 / “Q4 未实现”表述。

**ACCT：** 数字只引用队长已写入 `results_summary.md` 的条目。Q3/Q4 候选表见本文 §3，在签收前只允许当对账草稿。

**Codex：** 复核 PR #17 的物理/账本/前缀锁定；关闭讨论稿 D3/D4 勾选与规格的不一致；不要新开模型。
