# Cursor 交接单：Q2 政策一致重设计

**执行人：** Cursor（实现、测试、全年运行、图表）

**建模规格：** `docs/specs/q2-policy-consistent-redesign.md`

**禁止覆盖：** 现有 `output/q2_full_k8_risk/`、`output/result2.xlsx`、Q2 已签收图表和旧结果。

## 目标

实现“基准预测日前 LP + K=8 风险储备 + 逐 10 分钟残差匹配 MPC”。修复旧 `_validation_score` 使用整天真实路径 LP 的问题：候选风险分位的评分必须调用与正式运行一致的闭环 MPC。

## 实现任务

1. 新建独立模块/入口，不修改旧 Q2 生产入口的语义；建议输出根目录为 `output/q2_policy_consistent/`。
2. 新日前求解器只在 \((\hat L,\hat P)\) 上优化一条基准轨迹，保留 `q>=R(alpha)`、`x<=q`、公共母线、SOC/功率边界、预购按 \(q\) 计费和紧急电价。不得创建情景专属的充放电/SOC 变量。
3. 保留既有 K=8 medoid 和前缀权重函数，仅用于风险储备与 MPC 未来点预测。
4. 新建决策导向的闭环校准器：实现三种透明预测结构 `m1`（当前均值）、`m2`（同样窗口中位数）、`m3`（同样窗口近期加权均值）；对每个 \((m,\alpha)\) 组合在此前 14 个历史日完整运行“预测—锁定 q—逐 10 分钟 MPC”，连续传递该候选的 SOC，并以实际总成本 \(\sum(pq+5pe)\) 选择组合。禁止调用将全天 `data.load[i]`、`data.pv[i]` 直接作为未来优化输入的固定计划全视域调度器。
5. 下一日 value cuts 改为由同一基准预测日前 LP 构造；不能使用旧的情景全路径储能补救。
6. 显式记录 1 月预热回退日、回退来源、每天 SOC 和 2 月 1 日 SOC；实现至少一个 2 月初 SOC 边界敏感性，但不修改主路径初始规则。
7. 导出 MAE/净负荷 MAE/覆盖率作为诊断，但禁止以 MAE 为主选择准则。主评分须同时输出计划费、紧急费、未用计划量和总实际成本；在一倍标准误内的候选按较少未用计划量、再按 `m1`、再按较小 \(\alpha\) 打破平局。
8. 在主路径通过后，执行 \(K=4,8,12\) 的闭环敏感性；不能只报告 K=8。

## 分阶段执行与停止点

### C2-R1：独立骨架与日前 LP

建议新增 `q2/policy_consistent.py`、`q2/run_q2_policy_consistent.py`、`q2/validate_q2_policy_consistent.py`，并在 `q2/test_q2.py` 增加测试。旧的 `q2/pilot.py`、`q2/optimization.py` 生产语义及旧输出不得被重写。

完成以下事项后停止并提交：

1. `ForecastMode` 或等价配置对象实现 `m1/m2/m3`，保证每个历史来源日期可导出；
2. 新日前 LP 只接受一条 \((\hat L,\hat P)\) 轨迹及 \(R\)，代码和审计中都能证明不存在情景专属电池/SOC 决策；
3. K=8 只传入风险储备和残差匹配函数；
4. 新增单元测试全部通过，尤其是未来实际值扰动测试；
5. 输出 2025-02-01、2025-06-21 两日的 `forecast_policy_audit.csv`、调度台账和物理审计。

**此阶段禁止：** 年度运行、正式图、候选 `result2.xlsx`、修改旧 Q2 产物。

### C2-R2：闭环联合校准

仅在 R1 被 Codex 复核通过后开始：

1. 对每个 14 日校准块运行 12 个 \((m,\alpha)\) 候选；
2. 验证窗口内每个候选自行连续传递 SOC，窗口起点来自此前已部署主政策的已记录 SOC；
3. 以 \(\sum(pq+5pe)\) 的平均实际成本主排序，按规格的一倍标准误/未用计划电/简单模型规则打破平局；
4. 输出完整 `closed_loop_calibration.csv` 与 `forecast_diagnostics.csv`；
5. `policy_consistency_audit.json` 必须显式报告：校准和正式执行均未把验证日未来实际值输入优化问题。

**R2 交付后停止，等待 Codex 复核。**

### C2-R3：全年候选运行与敏感性

仅在 R2 被批准后：

1. 从 2025-01-01 的 6000 kWh 开始运行独立全年主路径，1 月预热与 2--12 月输出严格分栏；
2. 生成 2 月初 SOC 敏感性，以及 \(K=4,8,12\) 闭环敏感性；
3. 导出全年台账、物理审计、成本分解和与旧 Q2 的对照图；
4. 不导出 `result2.xlsx`，直至 Codex 完成年度复核。

### C2-R4：正式附件与图形

仅在队长确认主口径、Codex 年度复核通过后：生成候选 `result2.xlsx`、最终图和逐格对账。旧 `result2.xlsx` 必须保留可回退副本。

## 必须新增测试

1. 校准函数对验证日调用的执行器只能在每个时段读取当期实际值；扰动验证日未来实际负荷/光伏时，较早时段的计划和动作不得改变。
2. 日前求解器不存在形状为 `K × 144` 的情景专属 `charge/discharge/SOC` 决策变量。
3. 预测源日期、残差池日期和校准验证日均严格早于相应决策时点。
4. 计划 \(q\) 在日内哈希不变，且 \(x\le q\)、能量平衡、SOC 连续、功率/SOC 边界和无同时充放电均通过。
5. 2 月初 SOC 敏感性以相同 2--12 月日期、价格、预测和闭环策略比较；只改变明确登记的边界状态。

## 交付与验收

提交代码、测试和以下产物：

- `forecast_policy_audit.csv`、`closed_loop_calibration.csv`、`forecast_diagnostics.csv`、`policy_consistency_audit.json`；
- 全年 `daily_summary.csv`、计划/紧急购电台账、物理审计、运行元数据；
- K 敏感性与 2 月初 SOC 敏感性表；
- 主方案与旧 Q2 方案的成本、紧急购电、弃光、SOC 对比图；
- `validation.md/json`，明确是否生成候选 `result2.xlsx`。

先交付代码、单元测试和 2 个指定日期的闭环试运行；Codex 复核通过后才运行全年。全年审计通过且队长签收后，才导出新候选 `result2.xlsx`。

## 可直接发送给 Cursor 的 Prompt

> 请在独立分支实现 `docs/specs/q2-policy-consistent-redesign.md`，并严格按 `docs/handoff/cursor-c-q2-policy-consistent-redesign.md` 的 C2-R1 至 C2-R4 顺序执行。你只负责代码、测试、运行产物和图表；不要改写建模口径，不要覆盖 `output/q2_full_k8_risk/`、旧 `result2.xlsx` 或已签收图表。现在只执行 **C2-R1**：新增独立政策一致模块，日前 LP 只能在单一基准预测轨迹上产生锁定购电额度，K=8 只能用于风险储备和日内残差匹配；实现 `m1/m2/m3`，补未来实际值扰动测试与物理审计，并交付 2025-02-01、2025-06-21 两日结果。提交代码、测试输出、运行元数据和审计文件后停止，等待 Codex 复核；不得开始全年运行或导出候选 `result2.xlsx`。
