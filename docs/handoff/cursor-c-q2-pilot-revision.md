# Cursor 交接单：C 题 Q2 试算复核修订

**状态：** 已归档（历史试算修订）。相关修订、全年结果与 `result2.xlsx` 已完成并签收；不得将本任务单的旧限制覆盖当前规格。
**复核来源：** docs/reviews/q2-pilot-review.md。
**修改范围：** 仍限 q2/、output/q2_pilot/、fig/q2_pilot/；不得改写规格或复核文档。

## R1：使代码与试算输出一致

当前代码已加入 planned_q_sha256，但旧输出缺少该字段。

1. 固化当前 q2/forecast.py、q2/pilot.py 的改动；
2. 从当前代码重跑 P0–P4，覆盖旧的 pilot 输出；
3. q2.test_q2 单元测试和 q2/validate_q2_pilot.py 工件验证均须通过；
4. 回报提交号、重跑时间、两个命令的结论；不得只提交代码或只提交旧输出。

## R2：1 月联动预热

从 2025-01-01 的 6000 kWh 开始，用联动策略自身逐日运行至 2025-01-31：

- 早期历史不足时按已有回退预测/情景规则运行，并显式记录回退；
- 每一天的初始 SOC 必须等于联动策略前一天的实际末 SOC；
- 导出 linked_warmup_daily.csv，包含日期、SOC 首末值、计划成本、紧急成本、\(K\)、是否回退；
- 2025-02-01 的 P4 试算必须使用该预热末 SOC，而非完美信息比较器 SOC；
- 新增 SOC 连续、无未来数据和成本口径验证。

## R3：48 小时价值的最小实现

按规格第 3.5 节增加次日虚拟滚动：

- 日 \(d\) 的 \(q_{d,1:144}\) 是唯一锁定和实际结算的计划；
- 在 0:00 用当时可得历史生成 \(d+1\) 的预测/情景，构造内部次日成本到达项；
- 次日虚拟计划仅用于评价 \(E_{d,144}\) 的价值，不写入输出、不重复计费；
- 下一日 0:00 重新预测、重新锁定自己的 \(q\)；
- 对两个试算日重跑，额外报告“有 / 无 48 小时价值”的成本、日末 SOC和紧急购电差异；
- 所有次日预测仍不得使用 \(d\) 日或 \(d+1\) 日尚未发生的实际数据。

## R4：动态 \(K\) 冻结日历

实现完整运行日历：

- 每 14 天校准一次；
- 使用此前数据选择 \(K\)，并将该 \(K\) 固定用于随后 14 天；
- 导出 k_freeze_calendar.csv，列出校准日期、历史截止日、候选 \(K\)、入选 \(K\)、生效起止日期、验证成本、标准误和耗时；
- 早期样本不足时的候选缩减与回退必须可追溯；
- 不得每天使用当天重新选择的 \(K\)。

## R5：P1 比较器命名与边界

在代码输出和 README 中将 P1 标为“逐日完美信息比较器”，不得称“全局下界”。若要报告全局下界，必须以与主方案完全相同的 48 小时滚动口径另行计算并验证。

## 验收

- R1 工件验证通过；
- R2 的 2 月 1 日初始 SOC 可追溯至联动 1 月预热；
- R3 的次日价值不重复计费，且无未来数据泄漏；
- R4 冻结日历完整、可复现；
- 当前所有物理约束继续通过；
- 仅交付试算输出与复核报告，等待 Codex/队长批准后才运行全年正式计算。
