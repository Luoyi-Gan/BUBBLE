# 论文后续撰写计划

进度基准：2026-09-12 本次说明优先于旧交接文档。Q1--Q3 已完成当前可核验的模型正文；Q2 已采用政策一致闭环结果，Q3 已采用全年因果主结果，Q4 正在运行且尚未放行结果。比赛求解状态和论文审核状态分别管理。

## 当前交付范围

- Q1：问题分析、假设、符号、模型建立、求解算法、下界推导、结果与检验已整理为独立源文件并接入 main.tex。
- 四问问题重述：沿用已有补全稿，拆为背景和 Q1—Q4 五个文件。
- Q2：分析、模型、求解、结果与检验正文已接入；题设格式结果表和图表仍须从政策一致台账生成，不能回退使用旧方案数值。
- Q3：模型、求解、全年结果和检验正文已接入；题设格式结果表和图表仍须由正式 `result3.xlsx` 与年度台账生成。
- Q4：仅保留空白接口，等待运行完成及因果性、物理性、结算审计放行；不得用中间结果占位。
- 摘要、全篇评价、AI 使用声明及其他附录：保留框架，四问稳定后收尾。

## 推荐顺序（按交付件推进，不假设计算完成日期）

### 第 1 步：确认本次整合

检查 main.pdf 和 Q1 各审阅 PDF 的内容衔接、题目表头、公式、效率解释及时间标签。随后把新 ZIP 作为唯一 Overleaf 写作项目；旧包保留为备份。

### 第 2 步：补齐 Q2/Q3 的题设结果表与图表

材料先对齐：政策一致的 `result2.xlsx`、正式 `result3.xlsx`、相应年度台账、指定日期结果、成本分项、预测发布时间、调整记录和紧急购电记录。旧试算目录只可作回归参考，不能替代正式材料。

1. 依据正式工作簿生成 Q2/Q3 的题设格式结果表，统一日期、时段、单位与小数位。
2. 仅从已验收年度台账生成成本分项、紧急购电、SOC 与更新价值图；每幅图回链到源文件和策略口径。
3. 对正文数字执行一次工作簿--台账--图表的交叉核对；2--12 月输出区间与 1--12 月连续年度口径必须分列。

Q2/Q3 当前已接入主稿；补表、补图或修订文字均只修改同一源文件，避免生成平行版本。

### 第 3 步：Q2 先形成可执行的方法正文，结果等待计算

按 `01_analysis → 02_model → 03_solution` 撰写。以当前建模笔记为准，若仍采用“两阶段随机 LP + 滚动 MPC”，必须写清信息集、场景构造及权重、参数选择、终端价值显式形式、日初计划与日内执行关系。日初训练/调参只能使用当日零时前的数据，日内只能使用已发生或已发布信息。

计算完成后再写 `05_results` 和 `06_validation`：原题指定日期的三类表、result2.xlsx；如沿用消融任务，明确 334 天回测范围与全年题意的对应及1月初始化处理，输出各方案成本分项、紧急购电量、P95日成本。不能把“结果未生成”填为0，不能把旧 Q2 产物自动判定为当前方案结果。

### 第 4 步：Q4 方法与 Q2/Q3 对齐后成稿

先在 `sections/q4/01_analysis.tex`、`02_model.tex`、`03_solution.tex` 写波动电价造成的新增信息与决策变化，分别交代对 Q2 和 Q3 两条策略的改动。必须说明未来电价在决策时是否可知。

待两条计算链完成后，在 `05_results.tex` 分别报告对应 Q2、Q3 的结果，保持题目表格内容和表头，提供 result4-2.xlsx、result4-3.xlsx；在 `06_validation.tex` 对照固定电价与波动电价，解释成本变化和策略差异。

### 第 5 步：全篇收尾与最终合并

1. 各问确认后统一假设与符号：全篇通用量进入 `02_assumptions`、`03_notation`，局部量在本问首次出现时定义；保留效率、单位、时段及费用的一致口径。
2. 写 `sections/06_evaluation.tex`，将有数据支持的优点、不足与推广整理成全篇评价。
3. 最后写 `sections/00_abstract.tex`：每问按“问题—方法—关键结果—结论”压缩，只使用最终结果；确定题目与关键词。
4. 完成参考文献、实际 AI 使用记录和附录，不代填“未使用 AI”。
5. 全部审核通过后打开 Ready 开关，关闭 DraftNotes。连续编译并核查引用、分页、公式、图表、数字与题目原表；保留原始 xlsx 精度。

## Q1 尚需最终核对的事项（本次不重跑）

- 90% 的效率解释、公共母线计量侧与附件原始时段顺序沿用原方案，需在团队最终签收时统一。
- 下界推导已保留，但原包无 A_UB 或求解器对偶目标数值；尚不能报告经核验的数值 optimality gap。
- 旧图1仍带其他问题的 q_(n,t) 记号；图2沿用原图，其彩色坐标文字和首个时段状态的标注可在最终图件统一时再精修。本次没有伪造新图或改动图中数据。
- 原包5条参考文献保留；正式提交前逐一复核文献与论点的对应，补新模型来源。网络不可访问全文时不能声称已核对全文。

## 每部分的交付约定

内容文件 + 同源独立审阅 PDF + 对应真实图表/数据出处。新的图件遵循论文与科学绘图联动规则，保留可复现脚本，正文说明图所支持的结论；不为增加图片数量而画图。当前仅整理本地文件，不操作 GitHub；后续分支/PR 应按当时明确指令执行。
