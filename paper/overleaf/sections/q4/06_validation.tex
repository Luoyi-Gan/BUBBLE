价格预测训练样本严格早于决策时点，未来真实价格、负荷或光伏扰动不得改变已锁定的 $q$、$g^0$ 与已执行轨迹；价格日志可逐日复算候选 MAE 与选择结果。代表情景的 $(L,P,p)$ 来自同一历史日，保留相关性而不将其解释为因果机制。两类策略逐时段审计信息时序、$x\le q$、母线平衡、SOC、功率、弃光、跨日连续和充放电互斥；Q4-3 另核查 $g^F$ 只影响更新后时段，且始终相对 $g^0$ 结算。

365 日物理审计全部通过，跨日 SOC 最大衔接误差为 0，两个代表性日期的信息集扰动抽查通过；成本按式\eqref{eq:q4-2-actual-cost}、\eqref{eq:q4-3-actual-cost} 与正式日台账逐日核对。每个 $\alpha$ 候选均按“联合情景计划—锁定额度—10 分钟 MPC—连续 SOC”闭环评分；$K=4,8,12$ 的同协议复核支持保留 $K=8$（具体成本与耗时见表\ref{tab:q4-parameter}）。正式工作簿完整覆盖 2--12 月 334 日；与 Q3 固定价结果仅用于说明价格机制影响，不作优劣排序。
