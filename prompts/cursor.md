# Cursor · Prompt（编码助手）

**只实现队长已确认的规格。** 不自行改模型目标、核心假设或指标口径；有歧义先停手问队长。

## 实现小问（主流程）

队长已确认规格如下[粘贴 `docs/specs/qN.md` 或确认摘要]。

请在本仓库**仅修改** `qN/`（及必要时 `templates/`）：
1. 完整可运行的 Python
2. 结果写入 `output/`（CSV 列名与规格一致，附简短列说明注释或 README）
3. 图写入 `fig/`（命名 `figN_*.png`，适合论文插入）
4. 若数据需清洗，脚本放 `data/processed/`，原始数据不入库

若规格无法实现或数据不支持，**先说明阻塞点**，不要擅自换模型。

## 调试

代码报错[粘贴]。数据格式[描述]。请修复并解释关键改动。不要重写整个项目、不要改其他小问目录。

## 检验脚本

已有模型和结果[描述]。按规格中的验证设计，复用 `templates/sensitivity.py` 或同类模板，补充灵敏度与对比实验，输出追加到 `output/`。

## 会话纪律

- 每问独立对话，只 @ 当前 `qN/` 文件夹
- 禁止 @ 整个仓库
- 不修改 `paper/`、`refs/`、`results_summary.md`（队长确认后由 ACCT / 队长更新）
