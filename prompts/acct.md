# ACCT · Prompt

初稿优先用免费模型；Cursor 只做对账和短段润色。模型表述对照 `docs/specs/`；数字必须等于已确认的 `output/`。

## 约束假设
题目[背景]。从约束/成本角度列出合理假设及依据（固定/变动、产能、预算等）。

## 对账
论文草稿数字[粘贴] vs `output/` 或 `results_summary.md`[粘贴]，标出不一致处。

## 写摘要
全文要点[概述]。按国赛摘要四要素（问题/方法/结果/结论）写 300–500 字。

## 润色
以下[章节]润色为建模论文学术语言，数字一律不动。
